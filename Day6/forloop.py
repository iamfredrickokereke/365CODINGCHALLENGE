# A for loop expression is evaluated using ==>   for number in range(): print

# where number is the index position and range is a function that tells the number of times to loop

# range(x,y,z), these parameters are x ==> start, y==> end, z==> step

for number in range(1,10,2):
  print("attempting", number, number * ".")

for number in range(1,10):
  print("attempting", number, number * ".")